 现状（开发期）：写死的固定路径
    
    目前代码里是两处硬编码：
    
    - 工厂单据根目录：ground_truth.py 里 FACTORY_FOLDER = "/Users/nz/Downloads/yamato/96/工厂"，验证脚本同样写死
    - 下游表：GT_SOURCE = ".../96/ContentsOfTheContainer_202624_青島XD_20260708.xlsx"
    
    审核界面那条线稍有不同：app.config.settings.upstream_root 作为路径白名单根目录（防目录穿越），说明骨架线已经有"配置化"的雏形。
    
    生产建议：三层，不用每次对话设定
    
    1. 工厂单据根目录 → 配置项（固定默认值，可覆盖）
    
    96/工厂 这类根目录是相对稳定的基础设施，放进 settings / 环境变量（如 UPSTREAM_ROOT），启动时读一次即可。不需要每次对话设。LangGraph 的 Node2（文件夹路由）和审核界面的白名单共用这一个配置，保证"agent
    能看的"和"界面能显示的"是同一棵树。
    
    2. ContentsOfTheContainer 下游表 → 按批次传入（每批一参）
    
    这个文件名里带批次信息（202624_青島XD_20260708），每个航次/截单批次都会换。它属于业务输入而非配置：应在 POST /orders/process 的请求体里传入（thread_id 约定 ETD0725-工厂名 已经体现了批次概念），Node1
    解析后把 per-工厂 expected_skus 和表路径存进 LangGraph state，后续节点和 GT 校验都从 state 取，不再读全局硬编码。
    
    3. 单文件路径 → 随单据到达事件给出（增量模式）
    
    昨天定的增量模式下，agent 不需要"找"文件：process_file(session, file_path) 由调用方（监听目录的 watcher、人工拖入、邮件接收脚本）把路径递进来。agent
    只负责判定身份和提取。批量模式（extract_factory）只用于初始化/补录时扫整个文件夹。
    
    配套注意事项
    
    - 禁止按显示名硬编码任何文件名：真实文件名普遍含不间断空格 U+00A0（如 MX2-265584-002\xa0单据），所有定位必须走 glob/扫描/内容判定，这也正好和目标识别器"内容特征裁决"的设计一致
    - 别名映射表（app/alias_map.json）是另一层"位置"：下游表里的日文工厂抬头（山東中地）→ 本地文件夹名（中地），12 条人工映射，新工厂要维护——这个建议也配置化但持久保存，不随批次变
    - GT 校验用的路径和生产路径要分开：validation/ 里的硬编码可以继续留作测试基准，生产链路全部走上面三层
  
    一句话总结：根目录进配置、下游表随批次传、单文件随事件给——没有任何东西需要"每次对话设定"，也没有业务文件名应该写死。

