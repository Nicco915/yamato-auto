
现在是如何做到第一步识别出要提取的文件的，未来如何指引提取agent 识别出正确文件
  
    目前的做法：人工规则判定（我正在扮演的角色）

    这三个工厂里，"识别提取目标"是我在做，靠的是一套逐步积累的人工审查流程：

    1. 列出工厂文件夹全部文件（含子目录，注意 U+00A0 不间断空格）
    2. 逐个窥探内容而非看文件名/扩展名下结论：
      - xls/xlsx → 列出 sheet 名、打前几行
      - PDF → 抽文本层看开头几百字符
    3. 用"内容指纹"判定谁是箱单，已积累三类正反样本：

    ┌─────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
    │  判定信号   │                                                         证据                                                         │
    ├─────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
    │ ✅ 是目标   │ 标题含 "PACKING LIST"/"装箱单"，且有 13 位条码列 + 件数（packages/CTNS) + Net Weight + Gross Weight 列，SKU 级明细行 │
    ├─────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
    │ ❌ 报关版   │ 同有 PACKING LIST 标题，但只有品类汇总行、无条码（贝来 11A/11B；东基恒报关资料 PDF 只有 SANDBAG 845 CTNS 一行总数）  │
    ├─────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
    │ ❌ 采购订单 │ PDF 开头是 "PURCHASE ORDER SHEET"（CAINZ 抬头），有箱数无单重（贝来的 PDF；达安 jpg）                                │
    ├─────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
    │ ❌ 其他     │ 发票（INVOICE，有金额无重量）、入库导入模板（品类级一行）、合同（CONTRACT）                                          │
    └─────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

    4. 交叉验证：选定目标的合计件数/净重/毛重必须与 ground truth 合计吻合（贝来 191+85 箱、东基恒 845 PKGS/10733/11236 全部对上）

    未来如何指引提取 agent：两层结构

    第一层：确定性规则路由（不用 LLM，可单测）

    把上面的指纹写成代码化的评分规则，给每个候选文件打分：

    - 硬指标加分：文档内含 "PACKING LIST"/"装箱单" 标题；存在 13 位数字条码（正则 \d{13}）；存在净重/毛重表头关键词（Net Weight/N.W./净重、Gross
    Weight/G.W./毛重）；存在件数列（packages/CTNS/CARTONS/件数）
    - 硬指标排除：含 "PURCHASE ORDER"/"INVOICE"/"CONTRACT"/"SALES CONTRACT" 标题；条码数=0 且只有汇总行；文件名含"发票/合同/入库模板/报关"且无明细
    - 同票多份时取条码覆盖最多/行数最多的那份（贝来：清关单据 32 行 > 报关单据 23 行汇总）
    - 按票（PO 号/子目录）各选一份，合并后校验 SKU 覆盖率

    第二层：LLM 兜底 + 人工告知

    - 规则打分不清或得分为零时，把各文件的前 N 行文本给 LLM 做一次单选判断（输入很小、成本低），并要求它输出判断依据
    - 兜底出口：识别置信度低 → needs_human_review=True，走现有的人工审核界面让操作员直接指定文件——这就是你说的"人工告知后找到正确目标"，机制上已经存在（Node5 interrupt + resume）

    关键原则：目标识别和数值提取分离。识别层只回答"读哪份文件"，输出 source_file
    清单；提取层（已验证的四通道）只负责把给定文件吃透。这样每一层都能独立验证——就像我们这三次做的一样，先人工确认目标，再跑通道，错了能立刻定位是哪一层的锅。

    样本量建议：10 个工厂全过一遍人工核对后，识别规则基本能覆盖全部变体，再把规则固化成 target_router 模块并补单测。