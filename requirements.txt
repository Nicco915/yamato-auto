# 供应链单证自动化 - 依赖清单（宽松版本约束）
# 安装：pip3 install --user -r requirements.txt

# LangGraph 状态机与 SQLite checkpointer
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
langchain-core>=0.3

# Web 框架
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27            # 仅用于测试脚本打接口
python-multipart>=0.0.9  # FastAPI UploadFile / Form 解析依赖

# 数据库
sqlalchemy>=2.0

# 配置
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# 模糊匹配 / Excel / LLM SDK
rapidfuzz>=3.9
openpyxl>=3.1
openai>=1.30           # SiliconFlow 兼容 OpenAI SDK
pinecone>=6.0          # 调度 Agent RAG 向量库（KB_BACKEND=pinecone 时需要）

# 数据处理（系统已装，此处仅声明）
pandas>=2.0
pymupdf>=1.24
xlrd>=2.0            # 旧版 .xls 读取（pandas engine，见 extraction/excel_channel.py）
pypinyin>=0.50       # 报关明细普通行拼音排序（declare/aggregator.py）
